name: Election Poll

on:
  schedule:
    - cron: "1 1 1 1 1"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: 2026-bw-minute-poll
  cancel-in-progress: false

jobs:
  poll:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run election poller
        run: |
          python scripts/poll_election.py --election-key 2026-bw

      - name: Commit and push updates
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "github-actions@github.com"
          git pull --rebase
          git add README.md data/2026-bw config/2026-bw.json scripts/poll_election.py scripts/poll_election_core.py scripts/poll_ltw26.py
          git commit -m "2026-bw poll $(date -u +"%Y-%m-%dT%H:%M:%SZ")" || echo "No changes to commit"
          git push
